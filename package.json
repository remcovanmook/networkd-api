{
  "name": "networkd-api",
  "private": true,
  "version": "0.0.0",
  "description": "systemd-networkd API and frontend tooling",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build:schema-generator": "tsc -p tools/schema-generator/tsconfig.json",
    "generate-schema": "npm run build:schema-generator && node tools/schema-generator/dist/index.js manpages/systemd.network.html manpages/systemd.netdev.html > frontend/src/pages/schema.ts",
    "frontend:install": "npm --prefix frontend install",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "clean": "rm -rf tools/schema-generator/dist",
    "postinstall": "echo \"Root dependencies installed. Frontend has its own package.json.\""
  },
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "@types/node": "^25.0.9",
    "jsdom": "^24.0.0",
    "typescript": "^5.3.3"
  }
}
